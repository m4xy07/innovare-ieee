import { ShinyRotatingBorderButton } from "./shiny-rotating-border-button";

export const PreviewShinyRotatingBorderButton = () => {
  return (
    <ShinyRotatingBorderButton>
       Learn more
    </ShinyRotatingBorderButton>
  );
};

export default PreviewShinyRotatingBorderButton;
